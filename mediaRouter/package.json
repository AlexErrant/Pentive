{
  "name": "mediarouter",
  "version": "0.0.0",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20221111.1",
    "typescript": "^4.9.4",
    "wrangler": "2.6.2"
  },
  "private": true,
  "scripts": {
    "start": "wrangler dev",
    "dev": "wrangler dev",
    "deploy": "wrangler publish",
    "//generate-cert": "requires installing https://github.com/FiloSottile/mkcert Move `key.pem` and `cert.pem` to `~\\.config\\.wrangler\\local-cert`. (Apologies for the \\ escaping.)",
    "generate-cert": "mkcert -key-file key.pem -cert-file cert.pem api.local.pentive.com"
  },
  "dependencies": {
    "@planetscale/database": "^1.5.0",
    "hono": "^2.7.7",
    "jose": "^4.11.1",
    "shared": "workspace:*",
    "zod": "^3.19.1"
  }
}